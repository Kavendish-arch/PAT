#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int i,j,k,sum=0; 
	int n=10,a,count=0;
	for(i=0;i<n;i++)                       
     for(j=0;j<n;j++)                    //外循环为真进入，则n
    { a=0;				   
      for(k=0;k<n;k++)			
         sum++;		
     
	 } 
	 
	 printf("%d",count);                                 
	return 0;
}
